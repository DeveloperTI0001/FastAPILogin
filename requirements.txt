# requirements.txt
fastapi==0.108.0
uvicorn==0.27.0.post1
pydantic==2.5.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# ¡CRUCIAL! Reemplaza asyncpg por una versión binaria
# para evitar el error de GLIBCXX en Vercel.
# Usaremos 'psycopg' como alternativa más segura para PostgreSQL.
# Deberás adaptar tu código en 'src/db'
psycopg[binary,pool]