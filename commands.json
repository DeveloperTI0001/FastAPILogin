{
    "comandoIniciarLocal": "python -m uvicorn api.index:app --reload",
    "DespliegueVercel": "https://fast-api-login-six.vercel.app/",
    "comandoIniciarVercel": "No requiere comando",
    "instalarLibrerias": {
        "comando": "pip install",
        "librerias": {
            "fastapi": "pip install \"fastapi[standard]\" \"uvicorn[standard]\"",
            "supabase": "pip install supabase"
        }
    },
    "tenerEnCuenta": {
        "comandoIniciar": "El index:app depende del archivo que va a iniciar primero, en este caso index.py y app es la ruta @app"
    }
}